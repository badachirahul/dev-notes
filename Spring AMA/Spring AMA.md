# AMA

### 1. What problem does IoC (Inversion of Control) solve in Spring?
* `IoC solves the problem of tight coupling by transferring object creation and lifecycle management from the application code to the Spring container. Instead of classes creating their dependencies using new, Spring creates and injects the required objects using Dependency Injection.`

### 2. 