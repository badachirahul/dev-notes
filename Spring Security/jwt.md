# JWT

* `JWT is a token used for stateless authentication. After user login, the server generates a token containing user claims and signs it using a secret key. The client sends this token with every request, and the server verifies the signature to authenticate the user without storing session data.`